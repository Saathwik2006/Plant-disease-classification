# Plant-disease-classification
Built a CNN-based image classifier with resizing, normalization, augmentation, and batch training for multi-class plant disease detection. Applied transfer learning with pretrained ResNet by training the classification head and fine-tuning deeper layers to adapt learned visual features to the PlantVillage dataset.
